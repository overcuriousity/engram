services:
  qdrant:
    image: qdrant/qdrant:v1.18.2
    container_name: pkdb-qdrant
    ports:
      - "127.0.0.1:6333:6333"   # REST
      - "127.0.0.1:6334:6334"   # gRPC — the client uses this one
    volumes:
      - qdrant_data:/qdrant/storage
volumes:
  qdrant_data:
